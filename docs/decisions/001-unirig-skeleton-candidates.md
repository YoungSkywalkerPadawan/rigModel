# 001：独立准备 UniRig 骨架候选实验

日期：2026-09-17。

用户决定在新仓库接入 UniRig，并明确本轮使用 25 个既有持续旋转物体。先完成依赖、权重和适配脚本，再通知用户开启 GPU。

本轮保持官方源码和权重固定，以独立入口读取完整装配几何、调用官方骨架预测，并还原世界坐标。为降低 GPU 空转费用，点云、哈希校验和网络形状检查放在 CPU 阶段。备用选择是直接运行官方 Blender/Lightning 全流程；本次只需要位置候选，不引入蒙皮和完整动画导出依赖。

暂不训练或修改机械装配图。是否接入 RigAnything、训练节点关联器或替换 v29，等待本轮候选覆盖证据；这些后续研究决策尚未确定。
